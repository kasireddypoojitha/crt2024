#include <stdio.h>

int main() {
    int a;
    scanf("%d",&a);
    if(a%2==0)
    {
        printf("Even number");
        
    }
    else
    {
        printf("odd number");
    }
    
    // Write C code here
    printf("Try programiz.pro");

    return 0;
}
